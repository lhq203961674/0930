Linux環境では、tarコマンドで圧縮ファイルを解凍する際、展開先はtarファイルの保存場所ではなく、コマンドを実行したカレントディレクトリになる。  
そのため、フルパスでtarファイルを指定しても、解凍後のファイルは現在の作業ディレクトリに配置される点に注意が必要。  
解凍先を意図したディレクトリにする場合は、事前にcdで対象ディレクトリへ移動する、もしくはtarの -C オプションを利用して展開先を指定する方法がある。
